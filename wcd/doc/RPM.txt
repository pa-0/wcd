

RedHat RPM package installation
===============================

 Type:
   rpm -Uhv <rpm package file>

The wcd function for POSIX shells is defined in /etc/profile.d/wcd.sh, and an
alias in /etc/profile.d/wcd.csh for C alike shells.

