

RedHat RPM package installation
===============================

 Type:
   rpm -Uhv <rpm package file>

Wcd has by default been configured for bash.
The wcd function is defined in /etc/bashrc
For other shells read the manual section
INSTALLATION.

